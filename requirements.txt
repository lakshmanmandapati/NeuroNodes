# MCP Client - Node.js Dependencies
# This is a Node.js project, so dependencies are managed via package.json
# This file is for reference only - use 'npm install' to install dependencies

# Backend Dependencies (Production)
express==4.19.2                 # Web framework for Node.js
cors==2.8.5                     # Cross-Origin Resource Sharing middleware
body-parser==1.20.2             # Parse incoming request bodies
node-fetch==3.3.2               # Fetch API for Node.js (HTTP requests)
dotenv==16.3.1                  # Load environment variables from .env file

# Frontend Dependencies (Production)
react==18.2.0                   # React library for UI
react-dom==18.2.0               # React DOM rendering

# Development Dependencies
@vitejs/plugin-react==4.2.1     # Vite plugin for React
autoprefixer==10.4.19           # PostCSS plugin to parse CSS and add vendor prefixes
postcss==8.4.35                 # Tool for transforming CSS with JavaScript
tailwindcss==3.4.3              # Utility-first CSS framework
vite==5.2.0                     # Frontend build tool

# API Keys Required (set in .env file):
# OPENAI_API_KEY=your_openai_key_here
# GEMINI_API_KEY=your_gemini_key_here  
# CLAUDE_API_KEY=your_claude_key_here
# GROQ_API_KEY=your_groq_key_here

# Installation Instructions:
# 1. npm install                 # Install all dependencies
# 2. npm run dev                 # Start development server (frontend)
# 3. npm run server              # Start backend server
# 4. npm run build               # Build for production
# 5. npm run preview             # Preview production build

# Project Structure:
# /server/proxy.js               # Main backend server with dual-mode AI
# /server/intentParser.js        # Intent classification for chat vs tools
# /src/App.jsx                   # React frontend application
# /src/main.jsx                  # React entry point
# /src/styles.css                # Global styles
# /.env                          # Environment variables (API keys)
